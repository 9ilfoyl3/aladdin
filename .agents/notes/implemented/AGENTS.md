# AGENTS.md — Implemented Agent Notes

These notes describe shipped decisions. Follow the note format in [README.md](../README.md).

Keep paths, symbols, event names, defaults, and mechanisms synchronized with shipped behavior in the same change that alters them. Rewrite stale facts in place; do not append change history.

Changing a decision or its rationale requires a new note and a cross-link. Do not silently rewrite an implemented note into a different decision.
