# ML 模型依赖（本地 Embedding/Rerank 模式需要）
# 安装时指定 CPU 版 PyTorch 以减小体积：
#   pip install -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu

sentence-transformers>=3.4.0
transformers>=4.44.0,<5.0.0
torch>=2.5.0
numpy>=2.0.0
