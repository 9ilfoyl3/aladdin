/**
 * 运行时配置 —— 部署人员可直接编辑本文件，无需重新构建前端。
 *
 * 本文件会被原样拷贝到构建产物根目录（/config.js），并在 index.html 中
 * 于主程序之前加载。修改后刷新页面即生效（index.html 已配置 no-cache）。
 *
 * 字段说明：
 *   prismBackground —— 登录/注册/改密/接受邀请页左侧的动态背景渲染策略：
 *     'auto'   默认。自动检测设备性能，低端机或运行时帧率过低时降级为静态背景。
 *     'off'    强制关闭动态背景，所有设备都用静态渐变背景（现场机器普遍较差时用）。
 *     'on'     强制开启动态背景，跳过性能检测（确认现场机器性能足够时用）。
 */
window.__APP_CONFIG__ = {
  prismBackground: 'auto',
}
